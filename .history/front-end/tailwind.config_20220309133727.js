const config = {
  content: ['./src/**/*.{html,js,jsx,ts,tsx}'],

  theme: {
    extend: {
      fontFamily: {
        Sora: ['Sora'],
      },
    },
  },

  plugins: [],
};

module.exports = config;
