version: '3'
services:
  app:
    image: app
    build: ./front-end
    ports:
      - "3000:3000"
    networks:
      - notiefi


  api:
    image: api
    build: ./back-end
    ports:
      - "3001:3001"
    networks:
      - notiefi

networks:
  notiefi:
    driver: bridge